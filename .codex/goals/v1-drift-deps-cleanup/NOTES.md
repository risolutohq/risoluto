# NOTES

## Chronological Notes

- 2026-05-24 Goal compiled from Omer's provided scope. The request is execution-ready as a contract: it has scope boundaries, non-goals, scorecard, fast loop, final verification, and done_when criteria.
- 2026-05-24 `python3 /home/oruc/.codex/skills/goal-forge/scripts/inspect_codex_config.py --project-path /home/oruc/Desktop/workspace/risoluto-v1` reported `autonomous_goal_status: not ready`.
- 2026-05-24 Config gaps reported by the read-only checker: `model_context_window` unset, `model_auto_compact_token_limit` unset, `model_reasoning_effort` expected `high` but got `xhigh`, `approval_policy` unset, and `sandbox_mode` unset.
- 2026-05-24 The repo working tree was clean on `master` before adding goal scaffolding.
- 2026-05-24 Manual execution branch: `codex/v1-drift-deps-cleanup`.
- 2026-05-24 Both named Dependabot remote branches were already deleted/pruned. GitHub PRs #1 and #2 remain viewable as closed, unmerged PRs. Their package targets are already represented in current `master`: production (`better-sqlite3` 12.10.0, `express-rate-limit` 8.5.2, `liquidjs` 10.27.0, `yaml` 2.9.0) and dev (`@aws-sdk/client-s3` 3.1053.0, commitlint 21.0.1 packages, `@types/node` 25.9.1, Vitest 4.1.7 stack, ESLint 10.4.0, fast-check 4.8.0, knip 6.14.2, lint-staged 17.0.5, tsx 4.22.3, typescript-eslint 8.59.4, etc.).
- 2026-05-24 `pnpm outdated --format json` returned `{}`. `pnpm update --latest --lockfile-only` identified only manifest range drift for packages already resolved in the lockfile: `express` 5.2.1 and `@types/express` 5.0.6.
- 2026-05-24 Required drift search classification: remaining `frontend`/`docs-site`/`dashboard` hits are explicit exclusion/defer docs; `legacy` hits outside goal/docs are Express rate-limit `legacyHeaders: false`; `old` hits are ordinary variable/test names such as `oldStatus`, `oldestBacklogAgeSeconds`, and cooldown tests; `vite`/`vitest` hits are Vitest tooling/transitive lockfile context, not a frontend runtime; `playwright` remaining outside current surface is a transitive optional Vitest lockfile entry; `react` is the verb "reacts" in `src/git/pr-monitor.ts`; `anvil`, `risoluto-legacy`, `getrisoluto`, and `omerfarukoruc/risoluto` only remain in the goal contract after current-surface cleanup.
- 2026-05-24 Local Gitleaks binary is not installed (`gitleaks: command not found`). CI installs and runs Gitleaks 8.30.1.
- 2026-05-24 Omer widened the dependency instruction to update everything to latest and Corepack. Latest pnpm is `11.3.0`; `packageManager` now pins the Corepack-integrity form.
- 2026-05-24 pnpm 11 no longer reads `package.json#pnpm`, so repo pnpm settings now live in `pnpm-workspace.yaml`. The file is required for local and Docker frozen installs because it carries overrides and build-script approvals.
- 2026-05-24 Latest dependency state has `pnpm outdated --format json` returning `{}`. The only peer warnings are upstream metadata lag: latest `madge` and `type-coverage-core` do not declare TypeScript 6 support, but their commands used by this repo pass.
- 2026-05-24 Remote CI initially failed only on Dependency Review because GitHub reported Dependency Graph unsupported/disabled for this repo. A GitHub API attempt to enable `security_and_analysis.dependency_graph` did not make the field available. The workflow now keeps Dependency Review visible but non-blocking until the repository feature is available.
