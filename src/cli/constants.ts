/** Env var that opts the production CLI into the live agent-dispatch composition (real Codex spend). */
export const LIVE_RUN_START_ENV = "RISOLUTO_LIVE_RUN_START";

/** Fallback model name used when no per-profile model is resolved. */
export const DEFAULT_AGENT_MODEL = "gpt-5.4-mini";
