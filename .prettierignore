dist/
node_modules/
coverage/

# Obsidian Templater templates use EJS-style <%* ... %> tags that prettier mangles.
research/templates/
skills/risoluto-vault/assets/templates/

# Generated whole-project review artifact. Prettier is non-idempotent on its underscore-heavy
# inline text (e.g. RISOLUTO_STATUS), oscillating between _ and * emphasis, so it can never
# converge under --check; leave it as authored.
CODE-REVIEW-FINDINGS.md

# PRD docs mirror their Linear project — the prd-drift pre-push check is their source of truth.
# Prettier reformatting would diverge from the Linear content and fail that check, so leave PRDs
# formatted as authored/synced.
docs/prds/

# oxfmt formats the whole tree; keep these out of scope (research is a submodule).
research/
mockups/
interview/
skills/
.claude/
tests/fixtures/
